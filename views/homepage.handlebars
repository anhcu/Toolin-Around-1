<!-- Loop over each object in the projects array -->
<div class="row mb-4 project">
  <div class="col-md-5">
    {{!-- <h2>
        You are on the homepage of the template.
    </h2> --}}
    {{#if logged_in}}
      {{#each categories as |category|}}
      {{> category-details}}
      {{/each}}
    {{else}}
    <p>BLURBY BLURB BLURB BLURB</p>
    {{/if}}
  </div>
</div>



