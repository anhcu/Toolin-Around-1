<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Toolin' Around</title>
  <!-- LINK TO MATERIALIZE CSS FRAMEWORK -->
  <!--<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">-->
  <!-- LINK TO MATERIALIZE ICONS -->
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <!-- LINK TO CSS STYLESHEET -->
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2">
      <h1>
        <a href="/">Toolin' Around</a>
      </h1>
      <nav>
      <ul><a href="/">Home</a></ul>
      {{#if logged_in}}
      <ul><a id="logout" href="#">Logout</a></ul>
      <ul><a href="/toolbox">My toolbox</a></ul>
      <ul><a href="#">{{neighborhood_name}}</a></ul>
      <ul>
        <form id="search-form">
          <i class="red-text material-icons prefix">search</i>
          <input type="text" placeholder="search" id="search">
          <button type="submit" id="search-submit">Submit</button> 
        </form>
      </ul>
      {{else}}
      <ul><a href="/login">Login</a></ul>
      {{/if}}
      

      </nav>
    </header>
      <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  <script src="/js/search.js"></script>
 {{/if}}
</body>

</html>
