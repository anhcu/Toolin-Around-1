<!-- RENDERING A TOOL WITH NAME & USER THAT POSTED -->
<section>
    <a href="/tools/{{id}}">
      <div class="row tool-and-owner indigo-text text-darken-2">
        <h3 class="col s12 tool-name">{{name}}</h3>
        <p class="col s12">posted by {{user.name}}</p>
        <p class="col s12">{{category.name}}</p>
      </div>
    </a>
</section>
